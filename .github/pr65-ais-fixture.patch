diff --git a/tests/test_nmea_ais_enhancements.cpp b/tests/test_nmea_ais_enhancements.cpp
index 9c4c1ba..ba7cbdc 100644
--- a/tests/test_nmea_ais_enhancements.cpp
+++ b/tests/test_nmea_ais_enhancements.cpp
@@ -64,7 +64,10 @@ static uint8_t ais_text_value(char c) {
 }
 
 static void append_text(std::string& bits, const char* text, uint8_t chars) {
-    for (uint8_t i = 0; i < chars; ++i) append_bits(bits, text && text[i] ? ais_text_value(text[i]) : 32, 6);
+    const size_t text_length = text ? std::strlen(text) : 0u;
+    for (uint8_t i = 0; i < chars; ++i) {
+        append_bits(bits, static_cast<size_t>(i) < text_length ? ais_text_value(text[i]) : 32u, 6);
+    }
 }
 
 static std::string single_vdm_body(const AisPayload& payload, char channel = 'A') {
